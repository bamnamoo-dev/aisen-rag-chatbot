# 🏛️ AI-SENSE SMART RAG SYSTEM 사용자 및 관리자 통합 설명서

본 설명서는 **서울시교육청 교육행정 지침 기반 지능형 챗봇 시스템 (AI-SENSE SMART RAG SYSTEM)**의 원활한 운영 및 활용을 위해 작성되었습니다. 시스템의 기본 구성부터 신규 문서 분석, 고급 아키텍처 최적화 설정, 그리고 최근 반영된 프리미엄 UI/UX 세팅까지의 전 과정을 상세히 설명합니다.

---

## 1. 시스템 개요 (Overview)
AI-SENSE SMART RAG SYSTEM은 대용량 교육행정 PDF 지침서(수만 페이지 분량)를 의미 단위로 분할 분석하여 **로컬 FAISS 인덱스**에 저장하고, 사용자의 질문에 가장 관련성 높은 지침 단락들을 실시간 선별하여 **Google Gemini 2.5 Flash**를 통해 상세 해설을 제공하는 고성능 RAG 기반 지능형 서비스입니다.

---

## 2. 사용자 가이드 (User Guide)

### 2.1. 분야(카테고리) 선택 및 다운로드
1. **아이센스토어 ADMIN 브랜딩**: 웹 브라우저로 Streamlit 화면 접속 시, 좌측 사이드바 상단에 시그니처 코발트 블루 심볼(`I`)과 함께 `아이센스토어 ADMIN RAG SYSTEM` 로고가 노출되며, 아래에 **`ACTIVE CHANNELS`** 업무 채널 목록이 깔끔하게 표시됩니다.
2. **동적 아이콘 매칭**: 업무 카테고리 명칭(예: 공지사항, 업무공유, 자유게시판, 질의응답 등)에 알맞은 고유 이모티콘(📢, 🔗, 💬, ❓, 💼 등)이 시스템에 의해 자동으로 부여되어 카드 형태로 진열됩니다.
3. **가나다순 정렬 & '기타' 예외**: 업무 카테고리는 가나다 및 알파벳 순서로 일사불란하게 오름차순 정렬되며, **`기타`** 카테고리는 항상 전체 목록의 **가장 최하단**에 배치되어 업무 탐색의 직관성을 유지합니다.
4. **한 화면 초밀집 레이아웃**: Streamlit의 투박한 컴포넌트 여백을 완전히 걷어내어, 15~20개 이상의 많은 카테고리도 스크롤 바 없이 한 화면에 단정하고 밀도 있게 대시보드처럼 노출됩니다.
5. **활성화 및 유동형 지침서 다운로드**:
   - 카테고리를 누르면 선명한 코발트 블루 필(`background: #1e60ff`) 배경과 강렬한 섀도우를 띤 활성화 카드로 시각화됩니다.
   - 활성화된 카드 아래에 확장(아코디언)되는 PDF 지침서 목록은 파이썬 글자수 제한(12자)을 폐지하여 **전체 파일명**을 그대로 표시합니다.
   - **`CSS text-overflow: ellipsis`** 기술이 적용되어 화면 폭이 넓어지면 파일명을 최대한 길게 표시해주고, 화면이 좁아지면 알아서 예쁘게 말줄임표(`...`) 처리를 합니다. 리스트 내부 파일들 또한 완벽하게 **좌측 정렬**되어 시각적 안정감을 줍니다.

### 2.2. 인공지능과 대화하기 (Q&A)
1. 메인 화면 하단의 입력창에 업무와 관련된 궁금한 사항을 자유롭게 입력합니다.
2. 질문 입력 시, 검색 및 처리 진행 단계가 **실시간 Step Status (`st.status`)**로 화면에 시각화됩니다.
   - `🛰️ 1. 로컬 벡터 DB에서 관련 지침서 탐색 중...`
   - `📊 2. 유사도 기준 필터링 완료` (유사도 0.5 이상 선별 청크 수 표시)
   - `🤖 3. AI-SENSE 스마트 엔진이 답변을 작성하고 있습니다...`
3. 생성되는 답변은 다음과 같은 고유 구조를 갖습니다:
   - **💡 핵심 요약 (3대 지침 요점)**: 답변 맨 처음에 가장 연관성이 높은 3개 지침의 핵심 내용이 번호 매김 형식(1., 2., 3.)으로 요약되어 직관적인 해결책을 선제 제시합니다.
   - **🔍 상세 해설 및 분석**: 본문에서는 여러 법령 및 지침들을 상호 유기적으로 연결하여 문장형 해설을 전개하며, 주요 절차마다 참고 지침 파일명과 페이지 정보(예: `(지침서.pdf - 45p)`)를 명시합니다.
   - **📍 참고 지침 원문 확인**: 답변 하단에 선별된 원문 카드가 popover 배지 형태로 노출되어, 클릭 시 즉시 추출된 실제 원본 텍스트 및 표 데이터를 확인할 수 있습니다.
   - **💡 다음 질문 추천**: 답변 맨 하단에는 사용자가 함께 파악하면 좋을 관련 후속 질문 버튼 3개가 자동 제공됩니다. **하얀색 캡슐 배경에 코발트 블루 아웃라인과 블루 텍스트** 스타일로 디자인되어 마우스 오버 시 전체가 블루로 채워지며 위로 부드럽게 들려 올라가는 입체 애니메이션 효과가 들어있어 원클릭 대화로 연속성 있게 업무를 파악할 수 있습니다.

---

## 3. 관리자 가이드 (Admin Guide)

신규 지침서를 추가하거나 기존 PDF 문서 내용이 수정되었을 때 벡터 라이브러리를 최신화하는 과정입니다.

### 3.1. 지침서 PDF 파일 추가 및 갱신
1. 프로젝트의 **`manuals/`** 폴더 아래에 생성하고 싶은 분야 명칭으로 새 폴더를 생성하거나 기존 폴더에 진입합니다. (예: `manuals/학교회계/`)
2. 해당 분야 폴더 내에 공인 지침서 **PDF 파일들을 복사 및 저장**합니다.

### 3.2. 관리자 모드 활성화 및 분석 진행
1. 프로젝트 루트의 **`.env`** 파일을 텍스트 에디터로 엽니다.
2. `ADMIN_MODE` 환경 변수를 `True`로 변경한 후 저장합니다:
   ```env
   GOOGLE_API_KEY=your_gemini_api_key_here
   ADMIN_MODE=True
   ```
3. Streamlit 화면 새로고침하고 해당 카테고리 분야 버튼을 클릭합니다.
4. 시스템이 자동으로 파일 변경을 해시로 파악하여 **"⚙️ [관리자] 새 PDF 문서 의미 분할 및 벡터 분석을 진행합니다..."** 단계에 돌입합니다.
5. 화면에 노출되는 **실시간 진행률 바(st.progress)**와 현재 인덱싱 중인 파일명을 관찰합니다.
6. 인덱싱이 성공적으로 끝나면 사이드바에 `🎉 분석 결과 로컬 파일로 저장 완료!` 메시지와 함께 해당 폴더 아래에 숨김 처리된 직렬화 데이터베이스 파일인 **`.vector_cache.pkl`**이 자동 갱신 및 보존됩니다.
7. 분석이 완료되면 사용자 환경 보호와 서버 리소스 보존을 위해 `.env` 파일의 `ADMIN_MODE`를 다시 **`False`**로 복구하는 것을 권장합니다.

---

## 4. 고급 기술 사양 및 아키텍처

AI-SENSE SMART RAG SYSTEM은 고품질의 증강 생성을 구현하기 위해 고도의 설계 기법을 내장하고 있습니다.

### 4.1. RecursiveCharacterTextSplitter (의미 단위 분할)
- 일반적인 줄 바꿈 분할이 가진 한계를 해소하기 위해 `["\n\n", "\n", " ", ""]`의 우선순위를 갖는 파이썬 자체 구현 분할 엔진을 도입했습니다.
- **Chunk Size: 700자**, **Chunk Overlap: 80자** 규칙을 기반으로 문서의 문장이 도중에 잘려 의미가 훼손되는 것을 원천적으로 차단합니다.

### 4.2. PyMuPDF Table Layout Preservation (표 보존 기술)
- PDF 문서 내부의 수치화된 표 데이터를 무손실 상태로 임베딩하기 위해 `pymupdf`의 `find_tables` 기능을 전처리 단계에 결합했습니다.
- 추출된 셀 데이터를 정제하여 구조화된 **Markdown Table** 형식으로 재생성하고 이를 텍스트에 삽입함으로써 지침서 표 정보에 대한 초정밀 벡터 검색 및 생성 능력을 제공합니다.

### 4.3. FAISS + L2 정규화 코사인 유사도 검색
- NumPy 기반의 단순 연산을 탈피하여 대용량 벡터 처리에 압도적인 **FAISS (Facebook AI Similarity Search)** 알고리즘을 도입했습니다.
- 데이터베이스 빌드 시 임베딩 행렬을 L2 정규화(L2 Normalization) 처리하여 저장하고, 질문 입력 시 쿼리 벡터 또한 정규화한 뒤 `IndexFlatIP` (Inner Product) 인덱스 서치를 수행함으로써 수학적으로 완벽하고 초고속인 코사인 유사도 검색을 계산합니다.

### 4.4. 0.5 코사인 유사도 임계치(Threshold) 필터링
- 최상위 관련성 조각 `k=15`를 검색한 후, 유사도 스코어가 **0.5 미만**인 항목들은 필터링 단계에서 전면 배제합니다.
- 이를 통해 질문과 관계없는 불필요한 컨텍스트(Lost in the Middle)의 LLM 유입을 방지하여 처리 비용을 낮추고 답변의 정확성을 극대화합니다.
