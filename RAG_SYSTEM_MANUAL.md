# 🏛️ AI-SENSE SMART RAG SYSTEM 사용자 및 관리자 통합 설명서

본 설명서는 **서울시교육청 교육행정 지침 기반 지능형 챗봇 시스템 (AI-SENSE SMART RAG SYSTEM)**의 원활한 운영 및 활용을 위해 작성되었습니다. 시스템의 기본 구성부터 신규 문서 분석, 고급 아키텍처 최적화 설정, 그리고 최근 반영된 프리미엄 UI/UX 세팅까지의 전 과정을 상세히 설명합니다.

---

## 1. 시스템 개요 (Overview)
AI-SENSE SMART RAG SYSTEM은 대용량 교육행정 PDF 지침서(수만 페이지 분량)를 의미 단위로 분할 분석하여 **로컬 FAISS 인덱스**에 저장하고, 사용자의 질문에 가장 관련성 높은 지침 단락들을 실시간 선별하여 **Google Gemini 2.5 Flash**를 통해 상세 해설을 제공하는 고성능 RAG 기반 지능형 서비스입니다.

---

## 2. 사용자 가이드 (User Guide)

### 2.1. 분야(카테고리) 선택 및 다운로드
1. **아이센스토어 ADMIN 브랜딩**: 웹 브라우저로 Streamlit 화면 접속 시, 좌측 사이드바 상단에 시그니처 코발트 블루 심볼(`I`)과 함께 `아이센스토어 ADMIN RAG SYSTEM` 로고가 노출되며, 아래에 **`ACTIVE CHANNELS`**(지침서 탭) 또는 **`ACTIVE LAWS`**(법령/조례 탭) 목록이 깔끔하게 표시됩니다.
2. **법령/조례 이원화 네비게이션**: `⚖️ 법령/조례` 탭 선택 시, 사이드바에서 `⚖️ 상위법령`과 `🏛️ 자치법규` 카테고리 버튼을 활용해 국가 법령과 교육청 조례를 개별적으로 선택하여 해당 맥락에서 질문할 수 있습니다.
3. **동적 아이콘 매칭**: 업무 카테고리 명칭(예: 공지사항, 업무공유, 자유게시판, 질의응답 등)에 알맞은 고유 이모티콘(📢, 🔗, 💬, ❓, 💼 등)이 시스템에 의해 자동으로 부여되어 카드 형태로 진열됩니다.
4. **가나다순 정렬 & '기타' 예외**: 업무 카테고리는 가나다 및 알파벳 순서로 일사불란하게 오름차순 정렬되며, **`기타`** 카테고리는 항상 전체 목록의 **가장 최하단**에 배치되어 업무 탐색의 직관성을 유지합니다.
5. **한 화면 초밀집 레이아웃**: Streamlit의 투박한 컴포넌트 여백을 완전히 걷어내어, 15~20개 이상의 많은 카테고리도 스크롤 바 없이 한 화면에 단정하고 밀도 있게 대시보드처럼 노출됩니다.
6. **활성화 및 유동형 지침서 다운로드**:
   - 카테고리를 누르면 선명한 코발트 블루 필(`background: #1e60ff`) 배경과 강렬한 섀도우를 띤 활성화 카드로 시각화됩니다.
   - 활성화된 카드 아래에 확장(아코디언)되는 PDF 지침서 목록은 파이썬 글자수 제한(12자)을 폐지하여 **전체 파일명**을 그대로 표시합니다.
   - **`CSS text-overflow: ellipsis`** 기술이 적용되어 화면 폭이 넓어지면 파일명을 최대한 길게 표시해주고, 화면이 좁아지면 알아서 예쁘게 말줄임표(`...`) 처리를 합니다. 리스트 내부 파일들 또한 완벽하게 **좌측 정렬**되어 시각적 안정감을 줍니다.

### 2.2. 인공지능과 대화하기 (Q&A)
1. 메인 화면 하단의 입력창에 업무와 관련된 궁금한 사항을 자유롭게 입력합니다.
2. 질문 입력 시, 검색 및 처리 진행 단계가 **실시간 Step Status (`st.status`)**로 화면에 시각화됩니다.
   - `🛰️ 1. 로컬 벡터 DB에서 관련 지침서 탐색 중...`
   - `📊 2. 유사도 기준 필터링 완료` (유사도 0.4 이상 선별 청크 수 표시)
   - `🤖 3. AI-SENSE 스마트 엔진이 답변을 작성하고 있습니다...`
3. 생성되는 답변은 다음과 같은 고유 구조를 갖습니다:
   - **💡 핵심 요약 (3대 지침 요점)**: 답변 맨 처음에 가장 연관성이 높은 3개 지침의 핵심 내용이 번호 매김 형식(1., 2., 3.)으로 요약되어 직관적인 해결책을 선제 제시합니다.
   - **🔍 상세 해설 및 분석**: 본문에서는 여러 법령 및 지침들을 상호 유기적으로 연결하여 문장형 해설을 전개하며, 주요 절차마다 참고 지침 파일명과 페이지 정보(예: `(지침서.pdf - 45p)`)를 명시합니다.
   - **📍 참고 지침 원문 확인**: 답변 하단에 선별된 원문 카드가 popover 배지 형태로 노출되어, 클릭 시 즉시 추출된 실제 원본 텍스트 및 표 데이터를 확인할 수 있습니다.
   - **💡 다음 질문 추천**: 답변 맨 하단에는 사용자가 함께 파악하면 좋을 관련 후속 질문 버튼 3개가 자동 제공됩니다. **하얀색 캡슐 배경에 코발트 블루 아웃라인과 블루 텍스트** 스타일로 디자인되어 마우스 오버 시 전체가 블루로 채워지며 위로 부드럽게 들려 올라가는 입체 애니메이션 효과가 들어있어 원클릭 대화로 연속성 있게 업무를 파악할 수 있습니다.

---

## 3. 관리자 가이드 (Admin Guide)

신규 지침서를 추가하거나 기존 PDF 문서 내용이 수정되었을 때 벡터 라이브러리를 최신화하는 과정입니다.

### 3.1. 지침서 PDF 파일 추가 및 갱신
1. 로컬 PC의 구글 드라이브 동기화 폴더인 **`manuals/`** 아래에 새 분야 폴더를 생성하거나 기존 폴더에 진입합니다. (예: `manuals/계약/`)
2. 해당 폴더 내에 지침서 **PDF 파일들을 복사 및 저장**합니다.

### 3.2. 원클릭 자동 동기화 도구 (`sync.bat`) 사용법 (권장)
사용자의 수동 터미널 작업 번거로움을 줄이기 위해 **Zero-Touch 원클릭 자동 동기화 스크립트([sync.bat](file:///e:/내 드라이브/antigravity/sen-chatbot/sync.bat))**를 제공합니다.
1. `manuals/` 하위 폴더에 PDF 지침서 파일을 추가하거나 법규 최신화가 필요할 때 프로젝트 루트 폴더의 **[sync.bat](file:///e:/내 드라이브/antigravity/sen-chatbot/sync.bat)** 파일을 더블클릭합니다.
2. 스크립트 내부에서 국가 법령과 서울시교육청 자치법규 최신본을 다운로드합니다. 중앙 부처 법령은 `manuals/상위법령`에, API로 수집한 교육청 조례는 `manuals/자치법규`에 이원화하여 분리 저장합니다.
   * **스마트 로컬 캐싱**: 이미 다운로드된 법규는 최신 공포 정보(공포일자, 공포번호)를 비교하여 최신 상태이면 다운로드를 건너뜁니다. (전체 수집 속도 30초 ➔ 3초로 단축)
   * **네트워크 자동 복구**: 일시적인 SSL 에러나 구글 API 503 오류 발생 시 최대 4~10회 지수 백오프 자동 재시도로 인덱싱 작업을 중단 없이 완료합니다.
3. 스크립트가 모든 폴더의 변경 사항(파일 추가/수정/삭제 등)을 자동으로 스캔합니다.
4. **폴더 단위 메타 데이터 해시(파일명 목록 + 각 파일 크기)** 비교를 통해 변경된 카테고리만 콕 집어서 고속으로 벡터 DB를 재빌드합니다. (변경 사항이 없는 카테고리는 건너뜁니다.)
   * **오탐지 예방**: 파일 최종 수정 시각(`mtime`)이나 개별 파일의 단순 내용 갱신(legal_downloader로 인한 미세 정렬 변경 등)에 의한 오탐지를 원천 차단하고, 실제 파일 추가/삭제/크기 변경 시에만 동기화를 요구하도록 안정화되었습니다.
5. 캐시 파일(`.vector_cache.pkl`) 갱신이 끝나면, 변경된 파일들만 선별하여 **깃허브 원격 저장소(`main` 브랜치)에 자동으로 커밋 및 푸시(Push)**합니다.
6. 푸시 완료 후 약 1분 이내에 Streamlit Cloud 웹 클라우드 서버에 자동 배포 및 반영됩니다.

### 3.3. API 결제 등급 업그레이드 (Paid Tier 1) 관련 사항
* **배경**: 지침서 분량이 많아져 전체 문단(청크) 수가 구글 무료 API 일일 호출 제한(1,500회)을 초과하는 대규모 카테고리(예: '계약' 폴더 - 2,964개 청크)를 빌드할 때 요율 초과 오류가 발생할 수 있습니다.
* **조치**: Google Cloud 결제 계정을 정식 계정(일반 계정 활성화)으로 전환한 뒤, [Google AI Studio](https://aistudio.google.com/)의 `Settings` -> `Plan & Billing` 메뉴에서 프로젝트를 연동하여 **`Tier 1 (후불)`**로 등급을 업그레이드해야 합니다.
* **장점**: 결제 수단을 연동해도 사용 중인 무료 크레딧이 있다면 만료 시까지 우선 차감되므로 실제 요금은 청구되지 않으며, 분당 한도(1,500 RPM)가 대폭 완화되어 전체 벡터 빌드가 에러 없이 약 30초 내외로 초고속 처리됩니다.

### 3.4. Streamlit 웹 화면에서의 수동 분석 (과거 방식)
1. 프로젝트 루트의 **`.env`** 파일을 열어 `ADMIN_MODE=True`로 수정합니다.
2. Streamlit 화면을 새로고침하고 수동 분석을 진행할 카테고리를 선택합니다.
3. 화면에 나타나는 진행률 바를 확인하며 분석 완료 메시지가 뜰 때까지 브라우저 창을 유지합니다. (분석이 끝나면 `.env`를 다시 `ADMIN_MODE=False`로 돌려놓는 것을 권장합니다.)

### 3.5. 사용자 피드백 로그 및 만족도 리포트 확인 및 엑셀 다운로드
1. 관리자 비밀번호 입력 후 로그인(🔑)하여 메인 화면 우측 상단의 **`⚙️ RAG 관리자 대시보드`** 탭을 클릭합니다.
2. 대시보드 화면 맨 아래의 **`💬 4. 사용자 피드백 및 만족도 리포트`** 섹션으로 이동합니다.
3. 실시간으로 집계된 **총 피드백 수, 유용함(👍) 수, 수정 필요(👎) 수, 시스템 전체 만족도(%)**를 대시보드 메트릭 카드로 모니터링합니다.
4. 아래의 피드백 세부 데이터 표를 통해 사용자가 어떤 질문을 던졌고, AI가 어떤 지침서(참조 파일 리스트)를 근거로 어떤 답변을 작성했는지, 그리고 불만족한 구체적인 원인 코멘트를 역순(최신순)으로 정밀 스캔합니다.
5. **`📥 피드백 전체 기록 엑셀(CSV) 다운로드`** 단추를 누르면, 한글이 깨지지 않는 `utf-8-sig` 형식의 CSV 리포트 파일을 로컬 PC로 내보낼 수 있습니다.
6. 로컬 동기화 시 피드백 데이터(`manuals/feedback.csv`) 역시 `sync.bat`에 의해 원격 GitHub 저장소로 자동 커밋 및 배포되므로 별도의 DB 계정 없이도 영구 관리가 가능합니다.

### 3.6. FAQ (자주하는 질문) 캐시 관리자 및 원클릭 웹 서버 동기화
1. **FAQ 조회 및 편집**:
   - 관리자 로그인 후 메인 화면 우측 상단의 **`📑 FAQ 캐시 관리자`** 탭을 선택합니다.
   - 현재 시스템에 등록된 FAQ 목록(분야, 질문, 사전 답변 요약)이 표 형태로 표시됩니다.
   - 우측의 **`📝 수정`** 버튼을 눌러 인라인 에디터를 활성화해 질문 내용, 사전 답변, 카테고리를 편집하고 저장할 수 있으며, **`🗑️ 삭제`** 버튼으로 즉시 캐시에서 제거할 수 있습니다.
2. **원클릭 웹 서버 동기화 (Git Push)**:
   - 로컬 개발/관리 서버 환경(`.env` 파일 내 `IS_LOCAL=True`로 설정됨)인 경우, FAQ 캐시 페이지 하단에 **`🚀 웹 서버에 동기화 (Git Push)`** 영역이 나타납니다.
   - **`🚀 깃허브 원격 서버로 FAQ 동기화 시작`** 버튼을 클릭하면, 터미널 명령어를 입력할 필요 없이 변경된 FAQ 데이터 파일(`manuals/faq_db.pkl`)만 자동으로 Git staging 및 커밋(`Admin: Update FAQ database via dashboard sync`) 후 깃허브 원격 저장소(`main` 브랜치)로 백그라운드 푸시가 진행됩니다.
   - 푸시 성공 메시지가 나타난 후 약 1분 이내에 실시간 웹 서비스(Streamlit Cloud)에 연동 및 배포되어 즉시 반영됩니다.
   - *보안 가드레일*: 일반 웹 클라우드 서버 환경에서는 버튼이 자동으로 비활성화되며 보안을 위해 로컬 관리용 안내문이 노출됩니다.

---

## 4. 고급 기술 사양 및 아키텍처

AI-SENSE SMART RAG SYSTEM은 고품질의 증강 생성을 구현하기 위해 고도의 설계 기법을 내장하고 있습니다.

### 4.1. RecursiveCharacterTextSplitter (의미 단위 분할)
- 일반적인 줄 바꿈 분할이 가진 한계를 해소하기 위해 `["\n\n", "\n", " ", ""]`의 우선순위를 갖는 파이썬 자체 구현 분할 엔진을 도입했습니다.
- **Chunk Size: 700자**, **Chunk Overlap: 80자** 규칙을 기반으로 문서의 문장이 도중에 잘려 의미가 훼손되는 것을 원천적으로 차단합니다.

### 4.2. PyMuPDF Table Layout Preservation (표 보존 및 환각 방지 기술)
- **원자적 본문-표 분리 추출**: PDF 문서 전처리 과정에서 검출된 표 영역(`bbox`)을 추출하여 해당 바운딩 박스 안의 줄글 텍스트 블록은 본문에서 제외하고, 표 데이터 마크다운과 본문 일반 텍스트를 독립된 별개 청크(Atomic Chunk)로 분리 적재하여 데이터 중복을 없앴습니다.
- **표 내부 파이프 이스케이프**: 추출된 표 셀 텍스트 내부의 파이프(`|`) 기호를 역슬래시 이스케이프(`\|`) 처리하여 마크다운 셀이 열 경계를 무너뜨려 수치 정보가 한 열씩 밀려 환각을 일으키는 현상을 원천 방지했습니다.
- **분할 표 헤더 강제 복원**: 표 크기가 설정된 청크 제한을 초과해 여러 조각으로 분배될 때, 두 번째 조각부터 자동으로 상단 헤더 행(`| 항목 | 내용 |`) 및 구분선 행을 강제 삽입하여 LLM이 표의 속성과 열 의미를 온전히 해석하도록 보존합니다.

### 4.3. FAISS + BM25 로컬 하이브리드 검색 결합
- 의미 기반 코사인 유사도를 추적하는 **FAISS (Facebook AI Similarity Search)**와 핵심 텍스트 일치율을 정밀 스캔하는 **BM25 어휘 검색**을 하이브리드 융합(6:4 가중합)했습니다.
- 입력 질문에 대해 구글 API 임베딩 검색 후보군과 로컬 BM25 매칭 후보군의 **합집합(`union_indices`)**을 결합하며, FAISS 상위권 바깥의 청크는 직접 NumPy 내적으로 코사인 유사도를 계산하고, BM25 점수는 최대점 기준으로 정규화하여 가중합 점수($Score_{final} = 0.6 \times Score_{cosine} + 0.4 \times Score_{bm25}$)를 산출합니다.
- 이를 통해 조항 번호("제30조")나 고유명사("S2B") 등 텍스트 자체의 완벽한 일치가 중요한 행정 RAG 검색 정밀도를 200% 극대화했습니다.

### 4.4. 0.4 하이브리드 임계치(Threshold) 필터링
- 산출된 최종 하이브리드 점수가 **0.4 미만**인 노이즈 조각들은 RAG 입력 컨텍스트에서 전면 필터링하여 배제합니다.
- 이를 통해 질문과 무관하거나 유사도가 낮은 문단이 유입되어 발생하는 AI의 환각(Hallucination) 오류를 차단하고 답변의 신뢰도를 보장합니다.

### 4.5. 최신 지침 가중치 및 맥락 격리 (Score Boosting & Context Isolation)
- **최신 파일 자동 판별**: 폴더 내부를 스캔하여 파일명에 특정 키워드(`(2026)`, `(최신)`, `_new` 등)가 들어가 있거나, 파일 최종 수정 시각(`mtime`)이 가장 최근인 파일을 '우선순위 그룹'으로 지정합니다.
- **검색 후보군 확장**: 최신 지침이 초기 탐색 단계에서 탈락하는 것을 차단하기 위해, 기존 `k=15` 고정 검색에서 1차 후보군을 `search_k = max(50, k * 2)`로 확장하여 FAISS 탐색을 실시합니다.
- **점수 보정 및 파일명 파싱**: 대괄호 래핑이 적용된 메타데이터도 안전하게 식별하기 위해 `get_filename_from_metadata` 파서를 내장했습니다. 우선순위 그룹 파일에서 유래한 조각에 대해 유사도 점수에 **보너스 점수 `+0.1`**을 동적으로 가산합니다.
- **파일 경계선 기반 맥락 격리**: 검색 적중한 핵심 청크(`idx`)의 좌우 청크(`idx - 1`, `idx + 1`)를 엮을 때, 서로 다른 파일에서 유래한 텍스트 조각은 이어 붙이지 않도록 **파일명 일치 여부를 대조 검증**하여 타 지침 문서 간 맥락 오염(Context Pollution)을 완벽히 격리했습니다.
- **API 에러 시 키워드 매칭 스코어 동적 스케일링**: 구글 API 통신 불안정 또는 쿼터 소진 시 백업 키워드 검색을 실행하며, 매칭 단어 비율에 따라 스코어를 `[0.4, 0.8]` 구간으로 선형 보간 정렬함으로써 고정 점수로 인해 정렬이 무너지는 오류를 수정했습니다.
- **가드레일 프롬프트 보강**: 시스템 프롬프트에 충돌 방지 룰을 추가하여 행정 절차나 기준이 충돌하는 경우 상위에 배치된 최신 연도 지침서의 내용을 절대적 기준으로 삼아 답변을 구성하도록 가이드라인을 세웠습니다.

### 4.6. Streamlit Cloud 메모리 캐싱 및 실시간 핫 리로드 보장
- **핫 리로드 극대화**: Streamlit Cloud 환경에서 서브 모듈(`core/`, `services/`)의 수동/자동 코드 패치 시 캐시 무효화가 원활하지 않은 한계를 방지하기 위해, 진입 모듈인 `app.py` 맨 첫 단계에서 `importlib.reload()`를 탑재하여 모든 개발 소스 모듈을 강제 재로드하도록 처리하였습니다.

### 4.7. 보안 안전성 강화 및 Streamlit 버전 핀 고정 (2026-06-14 적용)
- **보안 자격 증명 폴백 제거**: `app.py` 소스 코드에 하드코딩되어 있던 기본 관리자 암호를 전면 삭제하고, 환경 변수(`ADMIN_PASSWORD`)가 세팅되지 않았을 때는 암호학적으로 안전한 32바이트 랜덤 토큰(`secrets.token_urlsafe(32)`)을 임시로 발급하여 비밀번호 매칭을 방지하고 시스템을 보호하는 방어적 폴백 로직을 구축하였습니다.
- **배포 라이브러리 버전 고정**: Streamlit 프레임워크 마이너 업데이트 시 UI 조작을 위한 내부 DOM 태그(`data-testid`)가 임의 변경되어 서비스가 중단되는 현상을 사전에 완벽 차단하기 위해, `requirements.txt`에 시스템 검증 버전(`streamlit==1.56.0`)을 영구적으로 핀(Pin) 고정하였습니다.
